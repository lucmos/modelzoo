name: "cifar10"
ref: "cifar10"
train_split: train
test_split: test
label_key: label
image_key: img
num_classes: 10
input_size: 32
in_channels: 3
transforms: ${..transforms}
standard_x_key: ${x_key}
standard_y_key: ${y_key}
